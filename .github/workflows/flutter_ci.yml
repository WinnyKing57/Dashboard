name: Flutter CI

on:
  push:
    branches: [ main ] # Or your primary branch, e.g., master
  pull_request:
    branches: [ main ] # Or your primary branch
  workflow_dispatch: # Allows manual triggering

jobs:
  build_and_test:
    name: Build Flutter App & Run Tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./flutter_dashboard_app # Updated default

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          # cache: true

      - name: List files in workspace (before pub get)
        run: ls -la flutter_dashboard_app # Adjusted to list inside the project dir
        # This will use the job default working-directory, but ls target is specified

      - name: Get Flutter dependencies
        run: flutter pub get
        working-directory: ./flutter_dashboard_app # Updated

      # Optional: Check formatting
      # - name: Check formatting
      #   run: dart format --output=none --set-exit-if-changed .
      #   working-directory: ./flutter_dashboard_app # Updated

      # Optional: Analyze code
      # - name: Analyze code
      #   run: flutter analyze
      #   working-directory: ./flutter_dashboard_app # Updated

      - name: Generate files (if any)
        run: flutter pub run build_runner build --delete-conflicting-outputs
        working-directory: ./flutter_dashboard_app # Updated

      - name: Run unit and widget tests
        run: flutter test
        working-directory: ./flutter_dashboard_app # Updated

      # Integration tests require an emulator or device, which is more complex for CI.
      # - name: Run integration tests (Placeholder - requires emulator/device setup)
      #   run: |
      #     echo "Integration test execution would go here."
      #     # flutter test integration_test
      #   working-directory: ./flutter_dashboard_app # Updated

      - name: Build Android APK (Debug)
        run: flutter build apk --debug
        working-directory: ./flutter_dashboard_app # Updated

      # Optional: Build Android App Bundle (Release)
      # - name: Build Android App Bundle (Release)
      #   if: github.ref == 'refs/heads/main' && github.event_name == 'push'
      #   run: |
      #     echo "Release build would go here."
      #     # flutter build appbundle --release
      #   working-directory: ./flutter_dashboard_app # Updated

      - name: Upload APK Artifact (Debug)
        uses: actions/upload-artifact@v4
        with:
          name: app-debug.apk
          path: flutter_dashboard_app/build/app/outputs/flutter-apk/app-debug.apk # Updated path
          # This path is now relative to GITHUB_WORKSPACE, and the APK is inside flutter_dashboard_app
